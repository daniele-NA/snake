/*
 * example_renderer.c - Original renderer_draw before game integration.
 * Cleared the screen and optionally drew a kotlin logo.
 *
 * This is reference code, not compiled into the project.
 */

#include "example_renderer.h"

/*
void example_renderer_draw(EGLDisplay display, EGLSurface surface, bool is_ready) {
    if (!is_ready) return;

    glClear(GL_COLOR_BUFFER_BIT);

    // draw_kotlin_logo(&renderer, 0.0f, 0.0f, 1.0f, 0.5f);

    eglSwapBuffers(display, surface);
}
*/
